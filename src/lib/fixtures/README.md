# Synthetic M1 attachment fixtures

Both files visibly identify themselves as synthetic Signal fixtures. They are original illustrations generated locally for M1 using ReportLab (PDF) and Pillow (PNG), with built-in PDF Helvetica and Windows Arial text rendering. No external diagrams, images, or production measurements were used.

The gateway diagram is illustrative only. The latency chart uses invented values of 84 ms and 52 ms. These files are not recovered originals. Their approved names match the accepted reference for attachment workflow testing.

Installation is debug/seed-only and uses the separate M1 fixture marker; later user removal is preserved.
