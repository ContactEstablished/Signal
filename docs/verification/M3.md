# M3 verification — complete and manually accepted


Status: **complete — user verification and approval received September 13, 2026.** The user stated: “M3 is verified. I approve. We need to work on M4 now. Let's commit and push what we have.” This closes the remaining manual acceptance gate and authorizes the M3 commit/push and M4 kickoff. User sign-off is distinct from agent-observed evidence: the historical exact-size and simulated-offline automation limitations below remain accurately recorded, not reclassified as automated passes.

Final checks: 79 frontend tests and 27 Rust tests passed; final dense-card changes additionally passed the 11 affected frontend tests. Type checking, Rust checking/formatting, Vite build, and the standalone Tauri debug build passed. No production changes followed those checks.

## User manual acceptance

The user also confirmed that, after Signal was hidden with close-to-tray enabled, choosing Open from the Signal tray icon restored the window. This is user-reported tray recovery verification; it does not assert a new Quit test or completion of the remaining M3 gates.

After the user confirmed the Schedule block edit was saved or canceled, native inspection found no open editor. Reloaded the seeded window with Ctrl+R and observed it finish loading Your Day without an error. The same five visible blocks and totals (6.0h planned, 4.8h logged, 1/5 complete) returned. The orange current-time marker is visually confirmed; accessibility output includes the Due soon scheduling buttons. This verifies a webview reload, not a full process restart or ordinary-database startup.

On September 13, 2026, the user confirmed the planner task picker works and subsequently confirmed all eight supplied M3 manual verification groups: move/resize and cancellation; overlap lanes; neutral block types; block timers/completion; removal preserving task/time/timer; automatic planning and repeat handling; date/project scope; persistence and unsaved-edit close prompts. These are user-reported results, separate from automated checks.

The same review requested compact block-detail actions. The refinement uses Lucide icons with Open, Edit, Complete/Reopen, Start/Pause/Resume/Stop, and Remove labels; Close is an accessible X button in the top-right corner. The dialog sizes to its compact action row within the viewport. The user screenshots show the pre-change Break and Parse config flags dialogs.

## Automated and native evidence

Before this refinement, all 24 Rust tests passed. The earlier full frontend run passed 61 tests; after adding nine planner component/state/integration checks, eight passed and one picker cancellation check failed. That check attempted cancellation before its asynchronous mock save had settled. It now explicitly verifies cancellation is blocked during saving, rejects the save, waits until Cancel is enabled, and verifies cancellation succeeds while the search is preserved.

Compact-action refinement checks (September 13, 2026):

- `pnpm check`: passed, 0 errors and 0 warnings. The first run identified an incomplete task fixture in `your-day.test.ts`; it now supplies the full task record rather than relying on an invalid type assertion.
- `pnpm test`: passed, 70 tests across 25 files, including the corrected picker cancellation check. `pnpm test -- tests/unit/your-day.test.ts` also passed both tests after completing that fixture.
- `pnpm build`: passed, 3,826 modules transformed. Vite reports a non-blocking bundle-size warning for the 578.29 kB main JavaScript chunk.
- Native source inspection: a read-only capture found the running seeded Your Day window. Another foreground window obscured its content, so no compact-dialog visual pass is claimed and no app data was changed during that inspection.

The earlier native seeded window displayed Your Day; automated native interaction was stopped by the user's physical Escape key. The final matched-size visual gate remains outstanding.

## Quick time and date editing — September 13, 2026

The user requested faster time changes and a date picker beneath Start/End. Implemented ±15-minute nudges, 30m/1h/1.5h/2h duration shortcuts, typed time validation, and a destination date for creation or movement. Saving an existing block retains its ID, completion/provenance, task and timer/history links, then opens its destination day. Date/time changes discard stale DST offset choices. The task due date is unchanged.

Verification after this refinement:

- `pnpm check`: passed, 0 errors and 0 warnings.
- `pnpm test`: passed, 74 tests across 26 files. New checks cover quick controls, end-of-day bounds, invalid typed times, date/offset validation, failed-save draft retention, and YourDay destination navigation/payload.
- `. ./scripts/dev-env.ps1` followed by `cargo test --manifest-path src-tauri/Cargo.toml --lib`: passed, 26 tests. New disposable-database tests cover cross-date identity/history/live-timer preservation, exact replay, changed-payload conflict, invalid dates, DST gaps/folds, and late-failure rollback.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` and `cargo check --manifest-path src-tauri/Cargo.toml`: passed.
- `pnpm build`: passed. Vite reports a non-blocking bundle-size warning for the 584.79 kB main JavaScript chunk.
- The existing seeded Tauri dev process rebuilt after the native change. Activated the rebuilt native window and opened Your Day → Testing Timer → Edit. Visually confirmed the compact five-action icon row in task details, Start/End nudge buttons, duration presets, and the date field below the times. Opened its native calendar and confirmed September 2025 appears. The dialog fits in the current approximately 1312×992 window. No saved fixture records were changed during this inspection; cross-date persistence was exercised in disposable database tests, not by moving the user's block.

The user subsequently confirmed this date/time refinement complete on September 13, 2026. This closes its user acceptance item. Native inspection confirms the editor and calendar render; running/paused action layouts and the complete matched-size milestone visual gate are not claimed by that inspection.

## Launch and review paths

```powershell
pnpm install --frozen-lockfile
. ./scripts/dev-env.ps1
pnpm dev:seed
```

The seed command expands to `tauri dev -- -- --seed`; it uses the existing isolated September 2025 fixture and advancing clock. Quit before switching to ordinary data with `pnpm tauri dev`.

For this refinement: Your Day → click a block title or its action chevron → inspect compact action buttons → close with the top-right X or Escape. Check a neutral block, an idle task block, and a running/paused task block. The underlying task, edit, completion, timer, and removal callbacks are unchanged.

For quick scheduling: Your Day → a block → Edit → use Start/End ± buttons or select a duration → choose Date → Save block. Confirm the destination day opens with the same block; reopen it to verify its saved range. Creation through Add block offers the same controls. Cancel keeps the saved schedule unchanged.

## Historical closeout work — resolved by final user acceptance

The remaining manual/native gate was accepted by the user in the final sign-off above. The evidence below records what the agent actually verified and where automation could not complete a check. No unexecuted automated check is claimed as passed.

Closeout review found roadmap discrepancies that are now corrected: Due soon scheduling, the orange now marker and gutter time label, Done today, the empty-day prompt, one-click Quick add, and task block prefix/time/logged-estimate metadata. Short blocks retain accessible details; medium-height blocks show the title and time without clipping full metadata. Final user sign-off now closes M3. M4 is authorized as the next phase; this M3 changeset contains no M4 production code.

## Due soon scheduling follow-up — September 13, 2026

The user confirmed Due soon rows only opened task details. Added the existing internal task drag contract to these rows and a keyboard-accessible + button that opens the scheduling editor for that exact task. The title still opens task details. Scheduling preserves the task's due date and uses the existing remaining-duration, date validation, persistence, and recovery guards. Both drag and + are disabled while planner writes/recovery block scheduling. The current-time marker and its dot now use the specified orange token; this marker appears only on clock-today at the current local minute, not on other selected dates.

Review path: Your Day → sidebar → Due soon → drag a task onto a time on the grid, or click its + → choose time/date → Save block. Add block → choose the task remains an alternative. Native drag/drop and the updated layout still need user/runtime confirmation; automated interaction checks are recorded separately.

- `pnpm check`: passed, 0 errors and 0 warnings.
- `pnpm test -- tests/unit/your-day.test.ts`: passed, all 5 tests. New integration tests exercise the Due soon + editor/save route and the internal drag-to-grid route with remaining duration, task due-date preservation, and recovery guards. This follow-up did not rerun the full suite or native tests; the preceding full results remain recorded above.

Later-phase stubs: Today/Week and meeting editing/recurrence remain M4; Daily summary remains M5; full tray/reminders remain M6; global header search remains M7. Assisted screenshot/text intake is proposed M8. No later phase is started by this refinement.

## Planner closeout additions — September 13, 2026

- Done today lists completed blocks from the selected date and project scope, including personal blocks. Other dates use “Done on this day.” Durations are planned block lengths, not invented logged entries. Past meetings are not classified as attended/completed; attendance remains M4.
- Empty scoped days show a dashed planning hint and due/copy preview actions with record-derived counts. The count explicitly includes overdue tasks. Existing past-date/no-source restrictions still apply.
- Quick add now submits one validated next-free proposal directly through the shared queue. It blocks repeated clicks while pending, reports no space without writing, and falls back to explicit offset review for ambiguous DST endpoints. Due/copy/carry previews retain their existing review flows.
- Due soon rows include project dots, grips, and remaining estimates; the now marker includes its local time label. Full task blocks include type, time, checkbox, title and logged/estimated hours; short/dense blocks keep disclosure access.
- A DST fold review found meetings could project as one minute when their wall-time end precedes their start. Both native occupancy and frontend projection now cover all occupied wall minutes across the fold. Planned totals still use actual clipped meeting duration, not that conservative visual envelope.

Automated evidence:

- `pnpm check`: final pass, 0 errors and 0 warnings. An intermediate test fixture supplied `null` for the non-null recurrence field; corrected it to `none` and reran the check.
- `pnpm test`: passed, 79 tests across 26 files. New checks exercise single-click Quick add, duplicate suppression, no-space handling, scoped completed/empty lists, and fold occupancy versus actual planned duration.
- After the final dense-card presentation adjustment, `pnpm test -- tests/unit/planner-canvas.test.ts tests/unit/your-day.test.ts`: passed, 11 tests.
- `cargo test --manifest-path src-tauri/Cargo.toml --lib`: passed, 27 tests, including native fold occupancy. `cargo check` and `cargo fmt -- --check` passed with the same manifest.
- `pnpm build`: passed; current main chunk 590.52 kB, with Vite's existing non-blocking size warning.

Native evidence:

- At the current approximately 1312×992 seeded window, inspected the updated Due soon and Done today lists. Order tile samples appeared as the single completed block at 1.5h.
- Navigated to September 13, 2025 and inspected the empty-day hint and due/copy actions. Clicked Quick add → Break once; no preview opened. The saved 07:00–07:30 Break is `4af69119-e00a-421b-9fe4-a22ed360ebf9`; a read-only query confirms exactly one block on that date. Left this test block in the fixture, with totals 0.5h planned / 0.0h logged / 0 of 1 complete.
- Rebuilt native startup displayed the new full and dense task card layouts and orange gutter time label. A border-drag sizing attempt left the captured dimensions unchanged; the subsequent native Size menu inspection did not establish either exact target size. No 1200×760 or 1600×960 pass is claimed.
- Temporarily disabled seeded close-to-tray, exited cleanly, and launched `. ./scripts/dev-env.ps1` then `pnpm tauri dev`. Ordinary Signal opened on September 13, 2026 with the real local clock, zero projects/blocks, and no fixture clock. Read-only database checks showed the fixture separately retained five projects, nineteen blocks, and its simulated clock. No ordinary tasks/projects were created. Closed the ordinary instance cleanly after inspection.
- `pnpm tauri build --debug --no-bundle`: passed, producing `src-tauri/target/debug/signal.exe` with embedded frontend assets. This is a standalone debug build, not a release installer verification.
- Offline startup remains blocked: automatic approval review rejected a proposed standalone seed launch with a process-local WebView2 proxy pointing to an unused loopback port and a fresh temporary WebView2 cache. The response provided only “blocked by policy.” The command did not execute, and no offline pass is claimed. The test mechanism was based on [Microsoft's WebView2 development flags](https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/webview-features-flags); no production flags or OS network settings were changed. Returned to the normal `pnpm dev:seed` workflow afterward.

Operational handoff: the test workspace was relaunched with `pnpm dev:seed`. Computer Use was stopped by physical Escape before the temporarily disabled close-to-tray preference could be restored; it remains OFF unless changed by the user. This does not alter its already verified opt-in behavior.
