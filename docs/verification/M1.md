# M1 execution and verification

Status: **complete — implementation and automated checks pass; all manual acceptance confirmed by the user.** September 12, 2026. Milestone commit and push authorized. M2 has not begun.

## Final user acceptance

On September 12, 2026, the user stated: “M1 manual acceptance is complete. I verified everything. Let's also commit and push this”. This completes the manual/native/visual acceptance gate, including the drag preview refinement. User verification is recorded as user-reported evidence, separate from the agent-observed checks below. The earlier automation limitation is historical and no longer blocks milestone completion. The authorized milestone commit is `feat(board): add projects tasks and task detail workflows`; it includes the four task/spec pairs and their implementation. No M2 work is included.

The user approved D1–D8 and explicitly authorized execution of all four task/spec pairs. The earlier kickoff created planning documents only. This execution adds production code, migrations, valid local fixture files, and meaningful tests. This report supersedes the kickoff's “implementation not started” status; M0.md remains historical evidence.

## Implemented boundary

| Pair | Production work | Acceptance status |
| --- | --- | --- |
| 1-1 | Migration 2, typed Rust workspace commands, project/task/child mutations, revision checks, manual ordering, confirmed graph deletion, managed attachment lifecycle, safe URL opening, native editor exit guard | Automated native checks pass; manual acceptance confirmed by user |
| 1-2 | Project create/edit/reorder/delete controls, five-column Board, AND/OR filters, pointer drag and keyboard status action, record-derived counts, project meetings strip | Native project creation, Board rendering and cross-column pointer move exercised; remaining manual/visual acceptance confirmed by user |
| 1-3 | New-task and detail dialogs, links, deadlines, blocked fields, subtasks/tags/alerts, sanitized Markdown, derived time rail, actual attachments, dirty/pending/failed close flows | Native detail/create/picker/open/remove exercised; remaining manual acceptance confirmed by user |
| 1-4 | Shared Svelte workspace, serialized writes, stale-read guards, committed-write/refresh-failure distinction, routing, native drop subscription, isolated synthetic fixture installation | Integrated; completion gate satisfied by automated results and user acceptance |

All writes use typed native commands against the SQL plugin's pool. Existing M0 tables/fields and migration 1 are preserved. Migration 2 adds task order/revision/blocked-since and the internal durable `attachment_file_ops` queue; it does not add timer, attendance, or notification schema. Opaque IDs, UTC instants, local block date/minutes, and the transactional hours cache remain intact. Project edits preserve summary configuration. Deletion previews fingerprint the affected graph and require renewed confirmation after changes.

File copying uses opaque managed filenames, a process-lifetime workspace lock, staging/adoption transactions, durable cleanup, and startup recovery. Logical removal can succeed with cleanup queued. File operations retain original source files. Notes strip scripts, handlers, images, and unsafe URLs; permitted links open externally through validated commands. No provider title fetching is implemented.

## Commands and recorded results

Final pre-commit rerun on September 12, 2026: `pnpm check` (zero errors/warnings), all 31 frontend tests, `pnpm build`, all 12 Rust tests, `cargo check`, and `cargo fmt -- --check` passed. Manual acceptance is user-reported; no new native UI automation was performed during this commit handoff.

Run from `C:\Projects\ContactEstablished\Signal`:

```powershell
pnpm install --frozen-lockfile
. ./scripts/dev-env.ps1
pnpm check
pnpm test
pnpm build
cargo test --manifest-path src-tauri/Cargo.toml --lib
cargo check --manifest-path src-tauri/Cargo.toml
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
pnpm dev:seed
```

`pnpm dev:seed` expands exactly to `tauri dev -- -- --seed`. Ordinary launch is `pnpm tauri dev`. Quit the running instance and stop its development command before switching modes; Vite uses port 1420. `pnpm dev` alone cannot verify native persistence.

| Check | Result |
| --- | --- |
| `pnpm check` | Passed, zero errors and zero warnings |
| `pnpm test` | Passed, 31 tests across 12 files (including the six M0 tests) |
| `pnpm build` | Passed; production frontend emitted successfully |
| `cargo test … --lib` | Passed, 12 tests |
| `cargo check …` | Passed with the installed MSVC toolchain |
| `cargo fmt … -- --check` | Passed |
| `git diff --check` | Passed; Git reports only local LF/CRLF conversion notices |
| Seeded `pnpm dev:seed` | Built and opened an actual decorated Windows/WebView2 window; Board, details and native picker worked before the automation stop |

Frontend tests cover calendar/DST deadlines, link recognition/hostile input, sanitized Markdown, filters/progress/meeting layout, mutation serialization, stale reads, committed writes followed by failed refresh, project-order publication, repeated creation suppression, retained failed drafts, pending child text, independent field validation, Enter/blur saves, and pending/failed native-close coordination. Component tests use a DOM and mocked callbacks; they supplement, rather than replace, the native tests.

Rust tests execute real SQLx transactions with the checked-in migrations in temporary databases. They cover status timestamps/revisions/no-ops, hidden-order preservation, project edits/order/validation, child normalization, untouched deadline precision, stale deletion fingerprints, graph unlinking, deletion rollback, reply-preparation rollback, first-seed ordering/marker rollback, attachment batch compensation, adoption rollback, recovery, root locking/isolation, and Windows locked-copy cleanup/retry. Exit-guard state tests cover repeated requests, stale replies, cancel, and original-intent preservation.

## Native evidence actually observed by the agent

This section preserves the original agent observations and their limits. References to pending reruns below describe that earlier session; subsequent manual acceptance was completed by the user, as recorded above.

- Existing M0 September fixture upgraded without resetting its records. Atlas Board displayed counts **3 / 3 / 2 / 1 / 2**; Today badge **3**; Atlas's September 11 meetings strip had **one** meeting. Fixture clock stayed **13:42 America/New_York**.
- ATL-482 detail opened with **8/12h**, five subtasks/three complete, notes, and both synthetic attachment records. Dialog and Board were inspected in the **1200×760 content** window with native Windows titlebar.
- Created disposable project **M1 Verification**, then task **M1 verification task** using `[M1 verification task](https://example.atlassian.net/browse/M1-1)` and Ctrl+Enter. Title and Jira metadata were parsed locally and persisted. Both records survived native rebuild/restart.
- The creation-completion path initially prompted to discard a successfully created link task. It was fixed with terminal completion state and tested for clean close and duplicate suppression. The corrected creation completion has automated coverage; its final native rerun remains pending.
- Pointer-dragged the disposable card from Backlog to In Progress in the Windows WebView. The card and counts moved; the database stored `in_progress`.
- Real Windows picker imported `src/lib/fixtures/latency-p95.png`. The database stored a new opaque managed copy, correct filename/MIME and **33,162 bytes**. Open launched the Windows default viewer (Snagit), which visibly displayed the synthetic graphic. Remove deleted the managed copy and metadata; the source remained present.
- Native Alt+F4 with an unsaved notes buffer was intercepted; Escape canceled closure and retained the editor. Its confirmation initially appeared below the scroll viewport. Scroll/focus handling was corrected and component close tests pass; a final native retest is pending.
- Fixed minimum-size Board overflow and overflowing additional project tabs during native testing. Later source-based refinements align the project sub-bar, meetings-strip caption/hour arrangement, card spacing, and controls with the accepted HTML. These final refinements have **not** been visually reverified.

## Database, files, and fixture reconciliation

These are historical agent snapshots, not a fresh census after the user's manual testing. The user subsequently confirmed the remaining manual checks.

Application data: `%APPDATA%\dev.contactestablished.signal`.

- Ordinary database: `signal.db`; managed root `attachments`; geometry `window.json`.
- Fixture database: `signal-dev-september-2025.db`; managed root `attachments-dev`; geometry `window-dev.json`.
- Ordinary database remained byte-for-byte unchanged through the recorded fixture work: SHA-256 `dedf2c4e177ac790f60ee12c42a71d4360f92ceec56a9986d0e5b64576ee7014`; zero projects/tasks/meetings/blocks/time entries/attachments. A final ordinary M1 launch is still pending, so its migration/open path is not claimed verified.
- Final read-only inspection: both databases `integrity_check = ok`; no foreign-key violations. Fixture has **5 projects, 17 tasks, 4 meetings, 13 blocks, 7 time entries, 2 attachments**, with **zero pending file operations**. The extra project/task are the explicitly created disposable verification records, not invented seed content.
- Seed baseline remains **4 projects / 16 tasks**. The new attachment marker installs two copies once, including on an existing M0 fixture. Multiple native launches preserved the two copies without duplication. Testing that a deliberately removed fixture attachment remains removed on restart is still pending.
- `gateway-arch.pdf` is **2,356 bytes**; `latency-p95.png` is **33,162 bytes**. Both were created locally, visibly labeled synthetic, and rendered/inspected. See the [fixture README](../../src/lib/fixtures/README.md). No original customer files are implied.
- Preserve the M0 reconciliation: no extra Backlog/Done cards, no Website Standup in Atlas, no fabricated timer history, auth **8h**, runbook **2h**, and unchanged original task/time-entry content. Date-only new deadlines default visibly to editable **17:00**; untouched existing instants preserve their precision.
- Sora 600/700 and DM Sans 400/500 local WOFF2 files remain bundled. M0's local-font verification is historical; the final M1 runtime font/network check has not been repeated.

## Historical automation limitation and manual acceptance checklist

During the original verification session, Computer Use stopped while entering a local accepted-reference URL into Microsoft Edge. Its automatic policy review could not determine the current browser URL confidently enough to enforce policy, and required stopping Computer Use for the turn. The browser connector also exposed no available browser surface. No alternative native automation was used to bypass the stop.

That session could not gather further native/visual evidence. The following checklist was handed to the user, who has now confirmed all manual acceptance is complete:

1. Render accepted internal #1b/#3a frames and compare the final app at **1600×960 and 1200×760**. Confirm fonts, final spacing/controls, keyboard focus, and offline behavior while localhost remains available.
2. Complete native project edit/color/reorder/delete (cancel and confirm), task graph deletion and next-project selection, and all task field families with reopen/restart. Use the existing disposable verification project. Remove it through Project menu → Delete project → review counts → Delete permanently after testing; original fixture records must remain.
3. Exercise filtered within-column/end/before-card drag, autoscroll/Escape, keyboard status changes, combined filters, and native restart after manual order changes.
4. Exercise real OS file drop in the same session as Board drag, outside-dialog rejection, confirmation gating, and Windows scaling above 100%; picker cancellation, missing/locked open and cleanup recovery in the UI; safe external task/note links. Picker/copy/open/remove already have the narrower evidence above.
5. Run native Close and tray Quit with clean, dirty, saving and failed project/task editors in both tray modes; verify Open restores a hidden window. The user's prior Open/Quit confirmation completed **M0**, not this new M1 draft guard matrix.
6. Launch ordinary mode separately and confirm migration/open with no fixture installation. Verify fresh temporary first-seed attachment installation and removed-fixture-attachment marker preservation, in addition to existing upgrade/repeat evidence.
7. Rerun relevant checks after fixes, review intended files, and make `feat(board): add projects tasks and task detail workflows` only after the completion gate passes.

The milestone commit was withheld while acceptance was incomplete. The user has now completed acceptance and explicitly authorized committing and pushing the M1 implementation and its paired planning documents.

## Drag preview review refinement — September 12, 2026

The user's M1 review replaces the bright insertion line with a translucent full-card destination preview. The source fades while dragging; releasing makes the destination card solid while the existing serialized move saves. Failed saves restore the original card and display the error. Escape, pointer cancellation, release outside the Board, and window blur remove the preview. Pointer capture is released, keyboard status controls remain available, and preview content is inert. The destination slot remains stable while its placeholder shifts surrounding cards.

Native check: moved the existing **M1 verification task** from In Progress to To Do, then back before the user's **Check the timers inM1** card. Both moves completed and displayed normal cards/counts. Original lane/order restored; the user's task content was preserved. The native tool performs each drag atomically, so it did not capture a separate mid-drag screenshot; the translucent hover state was subsequently included in the user's completed manual acceptance.

Automated verification after the refinement: `pnpm check` passed with zero errors/warnings; `pnpm test` passed 31 tests across 12 files. Regression coverage in `tests/unit/board-drag.test.ts`: real Board/component pointer handlers, preview content/inertness, stable anchor, solid pending-drop state, authoritative completion, rejected-save restoration, duplicate release, Escape/outside/pointer cancellation, pointer identity, and navigation during a pending save. This is component behavior coverage, not a claim that all remaining M1 native checks passed.

## Review click-paths and remaining stubs

- Review clarification (September 12, 2026): the task time card now labels **Start preview** / **Log preview** and explains beside the controls that they do not record time. Actual timer operation is not an M1 acceptance check. The requested live `HH:MM:SS` display is recorded in PLAN.md for M2.
- Header project → **Board**; **Filter** selects text, priorities, tags and date range. Group remains fixed to Status. Drag a card grip to reorder, or use its accessible status selector.
- Header **Add project** creates; project **…** edits name/color, moves left/right, or previews permanent deletion.
- Board **New task**, or `n` outside text fields. Paste URL/Markdown link; enter title/fields/children; Ctrl+Enter creates. Ctrl+Shift+Enter creates then opens the **Your Day preview**, without scheduling a block.
- Card → details. Text fields save on blur/Enter, selectors on selection; notes use **Edit → Save notes/Cancel**. Attachments **Add → native picker**, filename → external viewer, **×** → remove managed copy.
- Dirty close uses Discard/Keep editing for creation/project drafts and Save/Discard/Keep editing for details. Failed edits retain their buffers. Task and project deletion show affected counts and permanent wording.
- **Settings → Notifications → Keep running in the system tray** retains M0 preference behavior, default OFF. Native editor interception is added; the remaining manual regression matrix was subsequently accepted by the user.

Start/Log are explicit **M2** previews; Your Day planner **M3**; Week/meeting editing and Today digest **M4**; summaries/notifications/imports/backup/palette results and other Settings pages remain later previews. Meeting counts and linked meeting summaries are read-only. No timer, planner, notification, provider network fetch, or M2 workflow was implemented.
