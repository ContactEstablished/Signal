# M4 verification — Today, Week and meetings

Status: **implementation delivered; milestone partial pending required native/manual acceptance.** September 13, 2026. All three approved task/spec pairs have production implementations. The unattended audit is complete and ready for review; the milestone itself is not yet manually accepted. M5 has not started. On September 15, 2026 the user explicitly authorized committing and pushing the current development work. This supersedes the earlier commit deferral; it does not constitute manual acceptance or authorize a personal installation update.

## Delivered behavior

- Today uses the shared workspace clock and record-derived unfinished deadline buckets, Monday–Sunday strip, meetings, completed weekly hours, and existing-task Plan actions.
- Project Week has seven date columns, Later/No date and earlier overdue tasks, meeting pills, deadline drag previews, a keyboard Move to date editor, precision-preserving UTC conversion, explicit DST choices, cancellation and focus restoration. The header stays outside calendar scrolling.
- Meeting creation/detail supports owning project, title, saved IANA zone, elapsed duration and overnight end label, agenda/shared Markdown, safe external links, linked tasks across projects, reminder preference, planner visibility, recurrence scope, previews, attendance and separate occurrence notes.
- Migration 5 adds versioned recurrence segments, ordinal occurrence refs, complete snapshots, relational task links, durable exclusions/cutoffs and write receipts. Original tables/fields and migrations 1–4 remain intact. Legacy initialization runs atomically after seed/clock initialization and preserves each stored UTC instant; repeat initialization does not change an established series zone.
- All meeting readers share native bounded occurrence projection. Hidden meetings leave only planner occupancy/display/totals. Task detail uses a bounded linked-meeting week, not its deprecated empty `meetings` field.
- Agenda writes use the existing Workspace queue. Unknown results retain the exact request and lock competing writes until Retry. Known commits publish before refresh; no refresh failure silently repeats a write. Native exit guards include agenda drafts and recovery.

## Commands and automated evidence

Run from `C:\Projects\ContactEstablished\Signal` in PowerShell:

```powershell
pnpm check
pnpm test -- --maxWorkers=4 --minWorkers=1
pnpm build
. ./scripts/dev-env.ps1
cargo test --manifest-path src-tauri/Cargo.toml --lib
cargo check --manifest-path src-tauri/Cargo.toml
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
pnpm dev:seed
```

Quit that instance before ordinary startup:

```powershell
. ./scripts/dev-env.ps1
pnpm tauri dev
```

`pnpm dev:seed` expands to `tauri dev -- -- --seed` and uses `signal-dev-september-2025.db`; ordinary startup uses `signal.db`. The fixture calendar is September 2025 and advances from its persisted simulated clock. Do not delete either database or reinstall fixtures to repeat checks.

Observed results:

| Check | Actual result |
| --- | --- |
| `pnpm check` | Pass: 0 errors, 0 warnings. |
| `pnpm test -- --maxWorkers=4 --minWorkers=1` | Pass: 108 tests across 33 files after the final fixes. Worker limits only bound concurrency. |
| `pnpm build` | Pass. Vite retains a non-blocking main-chunk warning (645.53 kB, 166.55 kB gzip). |
| Rust unit tests | Pass: 40 tests, including uncertain receipt readback/replay regression. |
| `cargo check` | Pass. |
| `cargo fmt -- --check` | Pass. |
| `git diff --check` | Pass. |
| Ordinary `pnpm tauri dev` | Native Signal window opened cleanly on September 13, 2026; zero projects/tasks and no seeded meeting. Exited normally. |
| `pnpm dev:seed` | Native fixture window opened cleanly, including a full quit/relaunch after meeting edits. |
| Local font artifacts | All four WOFF2 files exist in `public/fonts` and the build's `dist/fonts`; CSS uses local `/fonts/` URLs. No font dependency or weight was added. Runtime font-face/network-panel inspection remains pending. |

Native regression tests cover additive migration/legacy fold preservation, exact-once receipt replay and late rollback, deadline precision and unrelated-field preservation, moved overnight exceptions, hidden planner occupancy, DST gaps without ordinal renumbering, stable distinct meeting identities, future Week versus actual Today, cancellation after recurrence changes, converted-series history, actual-past cancellation protection, stale review, cross-project task deletion, and reopening a temporary database after one-off deletion. Existing timer/planner/attachment/deletion tests remain green.

Frontend tests cover calendar precision/gaps/folds/defaults, read races, loading/recovery state, immutable retry, Workspace queue isolation and committed replies despite refresh failure, Today Plan callbacks, Week pointer preview/cancel/drop and keyboard editor, editable validation errors and meeting draft discard. An intermediate Week DOM assertion ran before the confirmed promise/render settled; corrected it to await the displayed result. An older transaction-failure test injected failure into raw meeting reads, which generic task detail no longer performs; its injection now targets alerts and still verifies rollback. No failure is counted as a pass.

## Native observations and disposable records

Native observations used the existing approximately **1311×992 outer-window capture** and preserved the user's five projects and edited fixture tasks/blocks.

1. Today, September 12, 2025: 3 overdue, 1 due today, initially 1 meeting. Done tasks remained struck through in the seven-day strip and absent from unfinished buckets. Hours reflected the edited fixture (Atlas 38.55h, Website 5.8h, Home 0h, CLI 18.04h, M1 Verification 0.03h), not invented reference totals.
2. Today → Meetings `+`: created **M4 disposable verification**, owned by Atlas Migration, September 12 at **14:15–14:45 America/New_York**, no recurrence, reminder 15. The save closed the form and both meeting list/strip updated to 2 meetings.
3. Opened that meeting, unchecked **Show in Your Day**, and saved. It remained in Today, Board's strip and Week, and was absent from Your Day. Saved the occurrence note **M4 native verification note. Keep this occurrence history.** with the separate attendance/notes action. Attendance remained Unmarked. Escape closed after save without a discard prompt.
4. Project Atlas Migration → Week: inspected seven columns, today's wash, Done styling, Later and No date. Opened **Write rollback runbook → Move to date**, saw September 12 and **17:00:00.000**, then Escape returned it to Friday without saving.
5. Today → **Write rollback runbook → Plan** opened Your Day's Schedule block editor with that existing task and September 12 selected. Cancel/Discard removed only this unsaved verification draft; no new planner block was saved.
6. Quit seeded Signal with Alt+F4, launched ordinary Signal, verified its separate empty workspace and actual calendar, quit, and launched `pnpm dev:seed` again. The disposable meeting appeared exactly once in Today's strip and meeting list after restart; its hidden planner setting remained effective. Existing edited fixture data remained present.
7. Native title remained `Signal · September 2025 fixture`. The new/detail editors use 460/760px widths and the detail has its 260px rail. Visual inspection prompted fixes to linked-task row direction, calendar scroll containment, and singular meeting copy. No horizontal page overflow was observed in the inspected Board/Week views.

The disposable meeting and the later audit task described below are intentionally retained for acceptance. No ordinary records were created. User task deadlines, planner blocks, time entries and timers were not changed by native verification; only the explicitly disposable task deadline was moved. The seeded development app is left running; close-to-tray remains OFF as it was at the accepted M3 handoff.

## Required acceptance still pending

September 13 acceptance follow-up: the reported missing Week card was traced with a read-only query to **My Test Task**, saved as `2026-09-18T21:00:00.000Z` (September 18, 2026, 17:00 New York). The visible week was September 15–21, **2025**, so its placement in Later was correct. No user deadline was changed. Later and earlier-overdue cards now show a full local deadline including the year. Confirmed the running native Week displays **Due Sep 18, 2026** on that card after hot reload. For a move into the displayed week, use September 18, **2025** (Thursday), or drag to the desired day. The initial follow-up did not commit a native drag; the later disposable-task audit below did.

Follow-up validation: `pnpm check` passed with zero errors/warnings; 12 Workspace/Week tests passed. Added coverage that refreshed snapshots replace the browsed Week after task edits and calendar moves, and that Later labels display the correct local date/year across UTC midnight. Native persistence logic was unchanged.

These are **not passed** by the automated suite or historical M3 sign-off:

- Exact **1600×960 and 1200×760** comparisons against accepted #2d/#3d/#3b. The current native border-drag attempt left the captured size unchanged at 1311×992; Computer Use provides no exact size setter. Verify header, seven columns, modal scrolling, footer access and no horizontal overflow at both sizes.
- Actual offline operation and runtime local-font loading. No OS network settings were changed. The current run checks local asset files/build output, not an offline WebView or its computed font faces. Keep the Vite loopback server running during an offline dev check.
- Fresh tray OFF/ON **Open/Quit**, dirty deadline Close/Quit, and dirty meeting tray Quit checks. Dirty meeting Alt+F4 → Keep editing was verified below. The window-scoped automation surface does not expose the Windows notification area; earlier user acceptance is historical evidence only. Exact unknown transport recovery is unit-tested, but no native IPC transport fault was injected into the user's workspace.
- Remaining native recurrence cases: a moved occurrence followed by a series edit, DST gap/fold entry, overnight rendering, external Join, reverse task → meeting navigation, and cancellation/non-resurrection. Weekly conversion, a future occurrence, cross-project meeting → task navigation and restart were observed below. Temporary-database tests cover the remaining persistence rules but do not substitute for each UI path.
- Native ghost-preview appearance during an active drag, dragging No date into a dated column, and M2/M3 timer/attachment/carry-over regression click paths. Committed dated moves, exact precision, keyboard focus, clearing and cancelling now pass in the native audit below. The automation captured the completed drag, not an intermediate ghost frame.

Suggested acceptance sequence (use disposable records):

1. Project Week → New task → make a disposable task with a deadline including seconds → drag its handle to another day → reopen and verify the time; Move to date → Clear deadline; drag from No date and review the visible 17:00 default; cancel once.
2. New task → enter a draft → Meeting switch → Keep editing/Discard; create an Atlas meeting linked to ATL-482 and a Website task. Use weekly recurrence, a saved zone and a safe HTTP(S) Join link.
3. Open one occurrence → save attendance and occurrence notes → move just that occurrence. Open an earlier occurrence → This and following → inspect retained override preview → apply. Verify the notes and moved exception are preserved.
4. Create an overnight 23:45 + 90-minute meeting; inspect both days and one stable detail. For a New York one-off, March 9, 2025 02:30 must be rejected; November 2, 2025 01:30 requires an explicit offset. A daily series skips the spring-gap slot and explains it.
5. Hide from Your Day and check planner space/totals while Today/Week/Board/task links retain the meeting. Cancel This occurrence / safe future Following → quit/restart → verify no resurrection. Delete only a disposable one-off and verify its linked task/block/log remains.
6. Test Join without automatically marking attendance; task detail → linked meeting → occurrence notes. Run two timers, pause/stop/log, planner Stop-and-complete, carry-over and a task attachment. Exercise tray Open/Quit, dirty drafts and both window sizes.

## Implementation notes and boundaries

### Unattended hardening pass — completed for review

The user authorized an unattended M4 audit on September 13, 2026. The goal is a review-ready handoff, not automatic milestone acceptance. This pass has reproduced and corrected:

- Saving meeting fields cleared unsaved occurrence notes/attendance. The separate draft now survives a normal save and exact receipt replay, remains protected by the discard prompt, and uses the returned revision when saved separately. Recovery reconciles the existing editor instead of remounting it and losing other drafts.
- Converting a one-off meeting to a recurring series incorrectly disabled its end-date and repeated-hour settings. These controls now lock only for an existing series' This occurrence scope.
- A database failure while retrying an uncertain request could discard its identity. AgendaState retains recovery in that case; native receipt readback failures report UnknownOutcome because the original write already committed. A real temporary-database regression injects a missing read table after creation, restores it, and verifies exact replay with only one meeting.
- Rapid repeated meeting submits are locally guarded while the original operation is unresolved.
- Week tried to focus a Move button while the pending mutation still disabled it. Focus now returns after the busy state clears and Svelte renders, including on a failed move. A regression assertion was observed failing before this change; native Space activation now opens the moved card’s deadline editor.
- Today’s relative hours bars used a minimum denominator of one hour, so a week with less than one hour never filled the largest bar. The largest nonzero total now fills its bar; all-zero totals remain empty. Added fractional-hour and all-zero regressions.

The new editor/recovery regression cases were observed failing before the fixes. Current full results: `pnpm check` passes (0 errors/warnings); `pnpm test -- --maxWorkers=4 --minWorkers=1` passes **108 tests in 33 files**; `pnpm build` passes (645.53 kB main chunk, 166.55 kB gzip; existing size warning); **40 Rust tests**, `cargo check`, and `cargo fmt -- --check` pass. These counts supersede the earlier implementation run above.

Native follow-up: opened the retained **M4 disposable verification** meeting from Today. Appended **Audit: retain this draft across Save meeting.** to its occurrence-note draft, changed duration from 30 to **45 minutes**, and clicked Save meeting. The editor retained the unsaved note; a read-only SQL query confirmed duration 45/revision 3 and the original saved note only. Native Alt+F4 displayed **Discard unsaved meeting changes?**; Keep editing preserved the draft and window. Save attendance & notes then persisted the full appended note at revision 4. Attendance remains Unmarked and Show in Your Day remains OFF. The meeting now ends at **15:00** on September 12, 2025. Only this previously identified disposable meeting was modified. Native capture in this pass is approximately 1534×1222, not evidence of the exact required dimensions.

Additional native evidence:

1. Inserted one clearly identified audit task as fixture setup (not a UI creation test): **M4 audit deadline precision**, ID `3919cac0-de37-48e3-9e3e-4e063d1a36aa`, Atlas Migration, To do. Its initial deadline was `2025-09-12T21:00:12.345Z`. The setup used the existing seeded database only, a transaction and foreign keys. No user task was edited.
2. Dragged that task Friday → Saturday. Read-only SQL confirmed `2025-09-13T21:00:12.345Z`, revision 1, unchanged status and hours. This exposed the disabled-button focus defect above. After fixing it, dragged Saturday → Sunday and pressed Space: the same card’s editor opened with September 14 and **17:00:12.345** New York. Clear deadline produced NULL/revision 3; Space reopened a visible **17:00:00.000** default. Escape cancelled without another write. `PRAGMA foreign_key_check` returned no violations. The task remains in No date.
3. Converted **M4 disposable verification**, ID `b339a3a1-e1e0-4ab4-a34d-0497a94a4890`, to Weekly using the native editor. End-date and fold controls became available. Selected **Hero section copy · Website Refresh** through the task picker, reviewed the change and applied it. Read-only SQL confirmed revision 5, weekly segment anchored at September 12, 2025 14:15, planner visibility OFF, and the cross-project link. Existing September 12 occurrence history and notes were retained.
4. Atlas → Week → September 15–21 showed the September 19 **14:15–15:00** occurrence. Its detail showed Weekly/This occurrence, separate empty occurrence notes and the Website link. Clicking that link opened **Hero section copy** task detail. Its linked-meeting list initially browses the clock’s current week (September 8–14), so an empty list there does not imply the September 19 link is missing. Reverse navigation via that list remains a manual check.
5. Closed the clean window with Alt+F4 and relaunched `pnpm dev:seed`. A new native window opened cleanly. Your Day still omitted the hidden audit meeting; Board retained it; Atlas Week → Next week again displayed the September 19 occurrence. The audit task remained in No date. No ordinary database was seeded and no fixtures were reset.

The code/coverage audit covered all three approved task/spec pairs, with regression evidence for deadline refresh, recurrence/DST identity, occurrence history, links, planner visibility, receipts and exit guards. Native automation supplied the observations above; it does not constitute full visual sign-off. Exact-size inspection could not be achieved with the window-scoped resize attempt, and this surface does not expose the Windows tray. Offline/runtime font inspection and the remaining UI scenarios were not observed and remain pending, rather than being inferred from automated tests. Native cancellation/deletion was left for attended acceptance: the Computer Use [SKILL.md](C:/Users/matth/.codex/plugins/cache/openai-bundled/computer-use/26.903.71938/skills/computer-use/SKILL.md) requires reading its confirmation policy, whose local app deletion rule says “Blocking confirmation required immediately before the action.” Temporary-database deletion/cancellation tests were completed independently.

### Return-to-app checklist

The seeded app is left open at **Atlas Migration → Week → September 15–21, 2025**, with no open draft. Its simulated clock continues advancing from September 12, 2025. Use `pnpm dev:seed` if it is closed.

- Review the visible September 19 **M4 disposable verification** meeting. Exercise one occurrence’s time/attendance/notes, then a following-series change and its preservation preview. Check reverse navigation from Hero section copy using the linked-meeting list’s Next week control.
- Use **M4 audit deadline precision** in No date for the remaining drag-preview/default-time visual check. Its current revision is 3 and it has no deadline, blocks or logged time.
- Finish the DST/overnight, safe Join, cancellation/restart and one-off removal steps in the acceptance sequence above using disposable records.
- Finish the two window sizes, offline/local-font runtime check, tray actions and M2/M3 smoke checks. No existing timer was started, stopped or reset by this audit.

These are the remaining M4 acceptance items, not M5 work. On acceptance, make the gated completion commit; no commit or push was performed during this unattended pass.

`MeetingDetail.series.is_recurring` is an intentional addition to the planned DTO: a series remains a series after its active tail stops repeating, so retained history can never become eligible for one-off deletion. Request outcomes retain affected task/project IDs for invalidation after deletion. Search uses a query-bound keyset cursor. Snapshot JSON has a dedicated typed shape without task IDs; relational links are authoritative. Day boundaries handle zones that advance at midnight. Reads are bounded to day/week and at most 10,000 projected occurrences; no unbounded future is materialized.

Reminder preferences are stored only (delivery M6). Daily summaries remain M5; full tray/scheduling M6; general palette/import/backup/polish M7; assisted screenshot/text intake M8. Project Notes remains its existing future placeholder. No meeting attachments, providers, invitation delivery or general task activity feed were added.

The original audit deferred its completion commit. The user subsequently authorized committing and pushing the current work on September 15, 2026. Remaining manual acceptance still applies, and M5 has not started. See the checkpoint evidence below.


## September 15 development checkpoint

The user requested “commit and push.” This checkpoint includes M4, the subsequent meeting weekday/time and combined-save improvements, planner/time-input and drag-feedback refinements, version 0.1.1 packaging configuration, and the development/personal separation instructions. The dated verification records above and alongside this file retain their historical evidence; this checkpoint does not claim new native visual acceptance.

Fresh checks against the complete current source:

- `pnpm check`: pass, zero errors/warnings.
- `pnpm test -- --maxWorkers=4 --minWorkers=1`: pass, **147 tests across 36 files**.
- `. ./scripts/dev-env.ps1` then `cargo test --manifest-path src-tauri/Cargo.toml --lib`: pass, **49 tests** using the default development configuration.
- `pnpm build`: pass; main chunk 656.25 kB / 170.10 kB gzip, with the existing non-blocking size advisory.
- Git whitespace validation: pass after removing two trailing blank lines flagged by the staged check.

This operation builds development assets only. No personal installer was built or run, no installed process was restarted, and no personal application data was opened or migrated. Database files, attachments, build output and installers are excluded from the commit.
